.venv/
__pycache__/
*.pyc
.env
*.log

# Dumps de backup do banco — contêm dados reais (pacientes, movimentações).
# Nunca versionar. docs/03_DEPLOY.md recomenda destino separado do
# servidor principal na instalação real; local aqui só para teste.
backups/
*.dump
